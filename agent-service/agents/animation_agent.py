"""Animation Generator Agent — emits a structured render_animation payload
(scene_type + steps) consumed by fixed frontend renderer components.
For real queries, translates EXPLAIN/.explain() output into steps."""
